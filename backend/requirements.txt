# Core web framework
fastapi>=0.117,<1.0
uvicorn[standard]>=0.37,<1.0
gunicorn>=23.0,<24.0

# Data processing
pandas>=2.3,<3.0
numpy>=2.3,<3.0
openpyxl>=3.1,<4.0

# HTTP & middleware
python-multipart>=0.0.20,<1.0
httpx>=0.28,<1.0
python-dotenv>=1.1,<2.0

# Observability
sentry-sdk[fastapi]>=2.49,<3.0

# Result caching
duckdb>=1.1,<2.0
filelock>=3.12,<4.0

# Task queue & caching
celery[redis]>=5.6,<6.0
redis>=5.0,<6.0

# Monitoring (optional — used by Flower dashboard)
flower>=2.0,<3.0

# S4PRED neural network inference
# Note: In Docker, use CPU-only torch via --extra-index-url
# (see Dockerfile.backend for details)
torch>=2.10,<3.0

# Vector similarity search — Wave 2 §D (ADR-016: LanceDB embedded supersedes Chroma)
# - lancedb: in-process columnar vector store at ./data/lance (no daemon, no port, no Docker sidecar)
# - sentence-transformers: local 384-dim all-MiniLM embeddings (CPU-fast, ~80MB model)
# - pyarrow: required by lancedb for columnar reads
lancedb>=0.6,<1.0
sentence-transformers>=2.2,<6.0
pyarrow>=15.0,<22.0
